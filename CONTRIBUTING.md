# Contributing to EAODS

Use short-lived branches such as `docs/<topic>`, `feature/<capability>`, `fix/<issue>`, and `chore/<maintenance>`.

Pull requests must explain business and architecture impact, identify affected EAODS volumes, pass documentation validation, preserve YAML front matter, and avoid secrets.

Use Conventional Commits.
